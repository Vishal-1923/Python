# Modules are used to borrow other's code.
# 2 types:
#   Built-in Modules : no need to install, comes directly with python interpreter
#   External Modules : need to install explicitly. via packet manager such as pip/conda.

# We use Modules as we dont want to do everything from scratch.

# Install Pip
# Pip can be installed via "sudo apt install python3-pip"
# Pip can be checked via "pip" or "pip3" command on terminal.

# any package/module can be installed via pip via
# pip install pandas
# is command s pip pandas lib internet s utha k python interpreter main install kr deta hai.
# if u r importing a lib without installing u'll get module not found error!
# install a library and for using it just import it.

